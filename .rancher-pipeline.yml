stages:
- name: build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: ssxs/jdk8-centos-ssxs:${CICD_EXECUTION_SEQUENCE}
      pushRemote: true
      registry: registry-vpc.cn-shenzhen.aliyuncs.com
timeout: 60
notification: {}
